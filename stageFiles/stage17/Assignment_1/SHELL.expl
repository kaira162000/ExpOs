int main()
{
decl
   	int temp;
	str fileName;
enddecl
begin
	temp = exposcall("Write", -2, "Enter program");
	temp = exposcall ("Read" , -1, fileName);
	temp = exposcall ("Exec", fileName);
	fileName="file not exist";
	temp=exposcall("Write",-2,fileName);
	fileName="/ not exec";
	temp=exposcall("Write",-2,fileName); 
	return 0;
end
}
